openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: ENGRAM AI Persona Framework API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: personality
    description: Personality trait operations
  - name: memory
    description: Memory layer operations
  - name: journal
    description: Reflective journal operations
  - name: personas
    description: Persona form operations
  - name: beliefs
    description: Belief registry operations
  - name: evolution
    description: Evolution log operations
  - name: initiative
    description: Initiative event operations
  - name: hiero
    description: Hiero-Code symbol operations
  - name: expressions
    description: Emotive QUERTY micro-expression library operations
  - name: stats
    description: System statistics and analytics
  - name: openai
    description: OpenAI AI chat operations
  - name: engrams
    description: Engram processing environment operations
  - name: media
    description: Multimodal media perception operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /personality:
    get:
      operationId: getPersonality
      tags: [personality]
      summary: Get personality traits
      description: Returns the current ENGRAM personality trait values
      responses:
        "200":
          description: Current personality traits
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonalityProfile"
    patch:
      operationId: updatePersonality
      tags: [personality]
      summary: Update personality traits
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PersonalityInput"
      responses:
        "200":
          description: Updated personality traits
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonalityProfile"

  /memories:
    get:
      operationId: listMemories
      tags: [memory]
      summary: List memory entries
      parameters:
        - name: layer
          in: query
          schema:
            type: string
            enum: [working, episodic, semantic, preference, reflective, procedural]
      responses:
        "200":
          description: List of memory entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MemoryEntry"
    post:
      operationId: createMemory
      tags: [memory]
      summary: Create a memory entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemoryInput"
      responses:
        "201":
          description: Created memory entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryEntry"

  /memories/{id}:
    delete:
      operationId: deleteMemory
      tags: [memory]
      summary: Delete a memory entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Memory deleted

  /journal:
    get:
      operationId: listJournalEntries
      tags: [journal]
      summary: List journal entries
      responses:
        "200":
          description: List of journal entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/JournalEntry"
    post:
      operationId: createJournalEntry
      tags: [journal]
      summary: Create a journal entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JournalInput"
      responses:
        "201":
          description: Created journal entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JournalEntry"

  /personas:
    get:
      operationId: listPersonas
      tags: [personas]
      summary: List all persona forms
      responses:
        "200":
          description: List of persona forms
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Persona"

  /personas/active:
    patch:
      operationId: setActivePersona
      tags: [personas]
      summary: Switch active persona form
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PersonaActivation"
      responses:
        "200":
          description: Updated active persona
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Persona"

  /beliefs:
    get:
      operationId: listBeliefs
      tags: [beliefs]
      summary: List all beliefs
      responses:
        "200":
          description: List of beliefs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Belief"
    post:
      operationId: createBelief
      tags: [beliefs]
      summary: Create a belief
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BeliefInput"
      responses:
        "201":
          description: Created belief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Belief"

  /beliefs/{id}:
    patch:
      operationId: updateBelief
      tags: [beliefs]
      summary: Update a belief
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BeliefUpdate"
      responses:
        "200":
          description: Updated belief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Belief"
    delete:
      operationId: deleteBelief
      tags: [beliefs]
      summary: Delete a belief
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Belief deleted

  /evolution:
    get:
      operationId: listEvolutionLog
      tags: [evolution]
      summary: List evolution log entries
      responses:
        "200":
          description: List of evolution log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EvolutionEntry"

  /initiative:
    get:
      operationId: listInitiativeEvents
      tags: [initiative]
      summary: List initiative events
      responses:
        "200":
          description: List of initiative events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InitiativeEvent"
    post:
      operationId: createInitiativeEvent
      tags: [initiative]
      summary: Log an initiative event
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InitiativeInput"
      responses:
        "201":
          description: Created initiative event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InitiativeEvent"

  /hiero-code:
    get:
      operationId: listHieroSymbols
      tags: [hiero]
      summary: List all Hiero-Code symbols
      responses:
        "200":
          description: List of Hiero-Code symbols
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HieroSymbol"

  /expressions:
    get:
      operationId: listExpressions
      tags: [expressions]
      summary: List all emotive QUERTY micro-expressions
      responses:
        "200":
          description: List of emotive expressions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Expression"

  /stats:
    get:
      operationId: getStats
      tags: [stats]
      summary: Get system statistics and analytics summary
      responses:
        "200":
          description: System analytics summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SystemStats"

  /openai/conversations:
    get:
      operationId: listOpenaiConversations
      tags: [openai]
      summary: List all conversations
      responses:
        "200":
          description: List of conversations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OpenaiConversation"
    post:
      operationId: createOpenaiConversation
      tags: [openai]
      summary: Create a new conversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenaiConversationInput"
      responses:
        "201":
          description: Created conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiConversation"

  /openai/conversations/{id}:
    get:
      operationId: getOpenaiConversation
      tags: [openai]
      summary: Get conversation with messages
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Conversation with messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiConversationWithMessages"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
    delete:
      operationId: deleteOpenaiConversation
      tags: [openai]
      summary: Delete a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /openai/conversations/{id}/messages:
    get:
      operationId: listOpenaiMessages
      tags: [openai]
      summary: List messages in a conversation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/OpenaiMessage"
    post:
      operationId: sendOpenaiMessage
      tags: [openai]
      summary: Send a text message and receive a streaming text response
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpenaiMessageInput"
      responses:
        "200":
          description: SSE stream of assistant text chunks
          content:
            text/event-stream: {}

  /engrams:
    get:
      operationId: listEngrams
      tags: [engrams]
      summary: List all engrams
      responses:
        "200":
          description: List of engrams
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Engram"

  /engrams/tick:
    post:
      operationId: tickEngrams
      tags: [engrams]
      summary: Run one autonomy tick across all enabled engrams (manual/dev trigger)
      responses:
        "200":
          description: Tick result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngramTickResult"

  /engrams/state:
    get:
      operationId: getEngramStates
      tags: [engrams]
      summary: Live autonomy state (per-drive pressure, cooldown, backoff) for every engram
      responses:
        "200":
          description: Live state for every engram
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EngramLiveState"

  /engrams/{id}:
    get:
      operationId: getEngram
      tags: [engrams]
      summary: Get a single engram
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Engram
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Engram"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
    patch:
      operationId: updateEngramConfig
      tags: [engrams]
      summary: Update an engram's designable processing-environment config
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EngramConfigInput"
      responses:
        "200":
          description: Updated engram
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Engram"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /engrams/{id}/activate:
    post:
      operationId: activateEngram
      tags: [engrams]
      summary: Make this engram the active chat engram (exclusive)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated engram
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Engram"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /engrams/{id}/transmit:
    post:
      operationId: transmitEngram
      tags: [engrams]
      summary: Force the engram to generate one transmission now (bypasses threshold)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Generated transmission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngramTransmission"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
        "503":
          description: Generation unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /engrams/{id}/transmissions:
    get:
      operationId: listEngramTransmissions
      tags: [engrams]
      summary: List an engram's transmissions (most recent first)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of transmissions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EngramTransmission"

  /engrams/{id}/transmissions/mark-seen:
    post:
      operationId: markTransmissionsSeen
      tags: [engrams]
      summary: Mark transmissions as seen
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkSeenInput"
      responses:
        "200":
          description: Count marked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarkSeenResult"

  /engrams/{id}/inquiries:
    get:
      operationId: listEngramInquiries
      tags: [engrams]
      summary: List inquiry history for an engram
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of inquiries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EngramInquiry"
    post:
      operationId: createEngramInquiry
      tags: [engrams]
      summary: Submit a probe or develop inquiry to an engram
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EngramInquiryInput"
      responses:
        "201":
          description: Inquiry result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngramInquiry"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /engrams/{id}/world-model:
    get:
      operationId: listEngramWorldModel
      tags: [engrams]
      summary: List an engram's world-model entries (most recent first)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of world-model entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WorldModelEntry"
    post:
      operationId: createEngramWorldModelEntry
      tags: [engrams]
      summary: Append a new world-model entry for an engram
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorldModelInput"
      responses:
        "201":
          description: Created world-model entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorldModelEntry"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /engrams/{id}/world-model/{entryId}:
    patch:
      operationId: updateEngramWorldModelEntry
      tags: [engrams]
      summary: Update an entry's content/confidence/scope/source (provenance is immutable)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: entryId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorldModelPatch"
      responses:
        "200":
          description: Updated world-model entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorldModelEntry"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
        "409":
          description: Provenance change rejected (provenance is immutable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
    delete:
      operationId: deleteEngramWorldModelEntry
      tags: [engrams]
      summary: Delete a world-model entry
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: entryId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /hub/spaces:
    get:
      operationId: listHubSpaces
      tags: [hub]
      summary: List all Hub spaces (display order)
      responses:
        "200":
          description: List of Hub spaces
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HubSpace"

  /hub/presence:
    get:
      operationId: listHubPresence
      tags: [hub]
      summary: List every engram's current presence in the Hub
      responses:
        "200":
          description: List of engram presence records
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EngramPresence"

  /hub/presence/{engramId}:
    put:
      operationId: moveEngramPresence
      tags: [hub]
      summary: Move (or place) an engram into a Hub space
      parameters:
        - name: engramId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MovePresenceInput"
      responses:
        "200":
          description: Updated presence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EngramPresence"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /hub/activity:
    get:
      operationId: listHubActivity
      tags: [hub]
      summary: List recent Hub activity (most recent first)
      parameters:
        - name: spaceId
          in: query
          required: false
          schema:
            type: integer
        - name: limit
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Recent Hub activity entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HubActivityEntry"

  /hub/controls:
    get:
      operationId: getHubControls
      tags: [hub]
      summary: Get the global autonomy controls (pause, quiet mode)
      responses:
        "200":
          description: Current global controls
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HubControls"
    put:
      operationId: updateHubControls
      tags: [hub]
      summary: Update the global autonomy controls
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HubControlsInput"
      responses:
        "200":
          description: Updated global controls
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HubControls"

  /messages:
    get:
      operationId: listEngramMessages
      tags: [hub]
      summary: List bus messages (engram-to-engram and human-directed), newest first
      parameters:
        - name: channel
          in: query
          required: false
          schema:
            type: string
            enum: [engram, human]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: Recent bus messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EngramMessage"

  /messages/mark-seen:
    post:
      operationId: markEngramMessagesSeen
      tags: [hub]
      summary: Mark human-channel messages as seen by the operator
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MarkMessagesSeenInput"
      responses:
        "200":
          description: Count of updated messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MarkMessagesSeenResult"

  /simulations:
    get:
      operationId: listSimulations
      tags: [simulations]
      summary: List bounded simulations, newest first
      parameters:
        - name: engramId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [proposed, running, paused, ended]
      responses:
        "200":
          description: Simulations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Simulation"

  /simulations/{id}/steps:
    get:
      operationId: listSimulationSteps
      tags: [simulations]
      summary: List a simulation's ordered steps
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Ordered simulation steps
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SimulationStep"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /simulations/{id}/control:
    post:
      operationId: controlSimulation
      tags: [simulations]
      summary: Operator control of a simulation (start, pause, resume, end)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SimulationControlInput"
      responses:
        "200":
          description: Updated simulation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Simulation"
        "400":
          description: Invalid transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /media:
    get:
      operationId: listMedia
      tags: [media]
      summary: List uploaded media assets, newest first
      parameters:
        - name: engramId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, processing, completed, failed]
      responses:
        "200":
          description: Media assets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/MediaAsset"

  /media/{id}:
    get:
      operationId: getMediaAsset
      tags: [media]
      summary: Get one media asset with the world-model observations it produced
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Media asset detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaAssetDetail"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
    delete:
      operationId: deleteMediaAsset
      tags: [media]
      summary: Delete a media asset and its bytes (world-model observations are preserved)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteMediaResult"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

  /media/{id}/retry:
    post:
      operationId: retryMediaAsset
      tags: [media]
      summary: Re-queue a failed media asset for perception
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Re-queued asset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaAsset"
        "400":
          description: Not retryable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenaiError"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    PersonalityProfile:
      type: object
      properties:
        id:
          type: integer
        curiosity:
          type: number
        humor:
          type: number
        stoicism:
          type: number
        empathy:
          type: number
        formality:
          type: number
        skepticism:
          type: number
        creativity:
          type: number
        initiative:
          type: number
        precision:
          type: number
        updatedAt:
          type: string
      required:
        - id
        - curiosity
        - humor
        - stoicism
        - empathy
        - formality
        - skepticism
        - creativity
        - initiative
        - precision
        - updatedAt

    PersonalityInput:
      type: object
      properties:
        curiosity:
          type: number
        humor:
          type: number
        stoicism:
          type: number
        empathy:
          type: number
        formality:
          type: number
        skepticism:
          type: number
        creativity:
          type: number
        initiative:
          type: number
        precision:
          type: number

    MemoryEntry:
      type: object
      properties:
        id:
          type: integer
        layer:
          type: string
          enum: [working, episodic, semantic, preference, reflective, procedural]
        content:
          type: string
        confidence:
          type: number
        tags:
          type: string
        createdAt:
          type: string
        lastAccessed:
          type: string
      required:
        - id
        - layer
        - content
        - confidence
        - createdAt

    MemoryInput:
      type: object
      properties:
        layer:
          type: string
          enum: [working, episodic, semantic, preference, reflective, procedural]
        content:
          type: string
        confidence:
          type: number
        tags:
          type: string
      required:
        - layer
        - content
        - confidence

    JournalEntry:
      type: object
      properties:
        id:
          type: integer
        event:
          type: string
        confidence:
          type: number
        reflection:
          type: string
        actionItems:
          type: string
        outcome:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - event
        - confidence
        - reflection
        - actionItems
        - createdAt

    JournalInput:
      type: object
      properties:
        event:
          type: string
        confidence:
          type: number
        reflection:
          type: string
        actionItems:
          type: string
        outcome:
          type: ["string", "null"]
      required:
        - event
        - confidence
        - reflection
        - actionItems

    Persona:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
        emphasis:
          type: string
        symbol:
          type: string
        isActive:
          type: boolean
        memoryBias:
          type: string
        reasoningStyle:
          type: string
      required:
        - id
        - name
        - description
        - emphasis
        - symbol
        - isActive
        - memoryBias
        - reasoningStyle

    PersonaActivation:
      type: object
      properties:
        personaId:
          type: integer
      required:
        - personaId

    Belief:
      type: object
      properties:
        id:
          type: integer
        statement:
          type: string
        confidence:
          type: number
        evidence:
          type: string
        counterarguments:
          type: string
        lastReviewed:
          type: string
        revisionCount:
          type: integer
        createdAt:
          type: string
      required:
        - id
        - statement
        - confidence
        - evidence
        - createdAt
        - revisionCount
        - lastReviewed

    BeliefInput:
      type: object
      properties:
        statement:
          type: string
        confidence:
          type: number
        evidence:
          type: string
        counterarguments:
          type: string
      required:
        - statement
        - confidence
        - evidence

    BeliefUpdate:
      type: object
      properties:
        statement:
          type: string
        confidence:
          type: number
        evidence:
          type: string
        counterarguments:
          type: string

    EvolutionEntry:
      type: object
      properties:
        id:
          type: integer
        revision:
          type: integer
        trigger:
          type: string
        description:
          type: string
        evidenceConsidered:
          type: string
        confidence:
          type: number
        expectedImpact:
          type: string
        validationOutcome:
          type: string
        createdAt:
          type: string
      required:
        - id
        - revision
        - trigger
        - description
        - confidence
        - createdAt

    InitiativeEvent:
      type: object
      properties:
        id:
          type: integer
        trigger:
          type: string
        message:
          type: string
        importanceScore:
          type: number
        confidenceScore:
          type: number
        noveltyScore:
          type: number
        overallScore:
          type: number
        wasDelivered:
          type: boolean
        createdAt:
          type: string
      required:
        - id
        - trigger
        - message
        - importanceScore
        - confidenceScore
        - noveltyScore
        - overallScore
        - wasDelivered
        - createdAt

    InitiativeInput:
      type: object
      properties:
        trigger:
          type: string
        message:
          type: string
        importanceScore:
          type: number
        confidenceScore:
          type: number
        noveltyScore:
          type: number
        overallScore:
          type: number
        wasDelivered:
          type: boolean
      required:
        - trigger
        - message
        - importanceScore
        - confidenceScore
        - noveltyScore
        - overallScore
        - wasDelivered

    HieroSymbol:
      type: object
      properties:
        id:
          type: integer
        glyph:
          type: string
        name:
          type: string
        meaning:
          type: string
        category:
          type: string
        compounds:
          type: string
      required:
        - id
        - glyph
        - name
        - meaning
        - category

    Expression:
      type: object
      properties:
        id:
          type: integer
        glyph:
          type: string
        name:
          type: string
        family:
          type: string
        eyes:
          type: string
        mouth:
          type: string
        gesture:
          type: string
          nullable: true
        valence:
          type: string
        arousal:
          type: string
        intimacy:
          type: integer
        cognitiveRole:
          type: string
          nullable: true
        notes:
          type: string
      required:
        - id
        - glyph
        - name
        - family
        - eyes
        - mouth
        - valence
        - arousal
        - intimacy
        - notes

    SystemStats:
      type: object
      properties:
        totalMemories:
          type: integer
        totalBeliefs:
          type: integer
        totalJournalEntries:
          type: integer
        totalInitiativeEvents:
          type: integer
        evolutionRevisions:
          type: integer
        activePersona:
          type: string
        avgBeliefConfidence:
          type: number
        memoryByLayer:
          type: array
          items:
            $ref: "#/components/schemas/LayerCount"
        initiativeByDay:
          type: array
          items:
            $ref: "#/components/schemas/DayCount"
        beliefConfidenceDistribution:
          type: array
          items:
            $ref: "#/components/schemas/ConfidenceBucket"
      required:
        - totalMemories
        - totalBeliefs
        - totalJournalEntries
        - totalInitiativeEvents
        - evolutionRevisions
        - activePersona
        - avgBeliefConfidence
        - memoryByLayer
        - initiativeByDay
        - beliefConfidenceDistribution

    LayerCount:
      type: object
      properties:
        layer:
          type: string
        count:
          type: integer
      required:
        - layer
        - count

    DayCount:
      type: object
      properties:
        date:
          type: string
        count:
          type: integer
      required:
        - date
        - count

    ConfidenceBucket:
      type: object
      properties:
        range:
          type: string
        count:
          type: integer
      required:
        - range
        - count

    OpenaiConversation:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        mode:
          type: string
        personaName:
          type: string
        customEngram:
          type: string
        engramId:
          type: integer
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - title
        - mode
        - createdAt
    OpenaiMessage:
      type: object
      properties:
        id:
          type: integer
        conversationId:
          type: integer
        role:
          type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - conversationId
        - role
        - content
        - createdAt
    OpenaiConversationInput:
      type: object
      properties:
        title:
          type: string
        mode:
          type: string
        personaName:
          type: string
        customEngram:
          type: string
        engramId:
          type: integer
      required:
        - title
        - mode
    OpenaiMessageInput:
      type: object
      properties:
        content:
          type: string
      required:
        - content
    OpenaiConversationWithMessages:
      type: object
      properties:
        id:
          type: integer
        title:
          type: string
        mode:
          type: string
        personaName:
          type: string
        customEngram:
          type: string
        engramId:
          type: integer
        createdAt:
          type: string
          format: date-time
        messages:
          type: array
          items:
            $ref: "#/components/schemas/OpenaiMessage"
      required:
        - id
        - title
        - mode
        - createdAt
        - messages
    OpenaiError:
      type: object
      properties:
        error:
          type: string
      required:
        - error

    VoiceProfile:
      type: object
      properties:
        speechStyle:
          type: string
        formatting:
          type: string
        vocabulary:
          type: array
          items:
            type: string
        sampleLines:
          type: array
          items:
            type: string
        narrationStyle:
          type: string
      required:
        - speechStyle
        - formatting
        - vocabulary
        - sampleLines
        - narrationStyle

    EmotionalBaseline:
      type: object
      properties:
        valence:
          type: number
        arousal:
          type: number
        volatility:
          type: number
        mood:
          type: string
      required:
        - valence
        - arousal
        - volatility
        - mood

    EnvironmentAnchor:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        locations:
          type: array
          items:
            type: string
        items:
          type: array
          items:
            type: string
        ambient:
          type: string
      required:
        - name
        - description
        - locations
        - items
        - ambient

    MemorySeed:
      type: object
      properties:
        relationship:
          type: string
        facts:
          type: array
          items:
            type: string
        summary:
          type: string
      required:
        - relationship
        - facts
        - summary

    Guardrails:
      type: object
      properties:
        framing:
          type: string
        boundaries:
          type: array
          items:
            type: string
      required:
        - framing
        - boundaries

    EngramDrive:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        description:
          type: string
        weight:
          type: number
        baseRate:
          type: number
      required:
        - id
        - label
        - description
        - weight
        - baseRate

    Engram:
      type: object
      properties:
        id:
          type: integer
        slug:
          type: string
        name:
          type: string
        title:
          type: string
        symbol:
          type: string
        origin:
          type: string
        voiceProfile:
          $ref: "#/components/schemas/VoiceProfile"
        emotionalBaseline:
          $ref: "#/components/schemas/EmotionalBaseline"
        environmentAnchor:
          $ref: "#/components/schemas/EnvironmentAnchor"
        memorySeed:
          $ref: "#/components/schemas/MemorySeed"
        guardrails:
          $ref: "#/components/schemas/Guardrails"
        drives:
          type: array
          items:
            $ref: "#/components/schemas/EngramDrive"
        focusThemes:
          type: array
          items:
            type: string
        autonomyEnabled:
          type: boolean
        tickCadenceSeconds:
          type: integer
        initiationThreshold:
          type: number
        driveState:
          type: object
          additionalProperties:
            type: number
        currentMood:
          type: string
        lastTickAt:
          type: string
        lastTransmissionAt:
          type: string
        backoffUntil:
          type: string
          nullable: true
        isChatActive:
          type: boolean
        mode:
          type: string
          enum: [orientation, social, simulation, initiative_limited, full_bounded, quiescent]
        humanContactEnabled:
          type: boolean
        simulationEnabled:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - slug
        - name
        - title
        - symbol
        - origin
        - voiceProfile
        - emotionalBaseline
        - environmentAnchor
        - memorySeed
        - guardrails
        - drives
        - focusThemes
        - autonomyEnabled
        - tickCadenceSeconds
        - initiationThreshold
        - driveState
        - isChatActive
        - mode
        - humanContactEnabled
        - simulationEnabled
        - createdAt
        - updatedAt

    EngramConfigInput:
      type: object
      properties:
        autonomyEnabled:
          type: boolean
        tickCadenceSeconds:
          type: integer
        initiationThreshold:
          type: number
        mode:
          type: string
          enum: [orientation, social, simulation, initiative_limited, full_bounded, quiescent]
        humanContactEnabled:
          type: boolean
        simulationEnabled:
          type: boolean
        focusThemes:
          type: array
          items:
            type: string
        emotionalBaseline:
          $ref: "#/components/schemas/EmotionalBaseline"
        drives:
          type: array
          items:
            $ref: "#/components/schemas/EngramDrive"

    Simulation:
      type: object
      properties:
        id:
          type: integer
        engramId:
          type: integer
        spaceId:
          type: integer
        premise:
          type: string
        status:
          type: string
          enum: [proposed, running, paused, ended]
        currentStep:
          type: integer
        maxSteps:
          type: integer
        stepCooldownSeconds:
          type: integer
        lastSteppedAt:
          type: ["string", "null"]
        exitSummary:
          type: ["string", "null"]
        startedAt:
          type: ["string", "null"]
        pausedAt:
          type: ["string", "null"]
        endedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - engramId
        - spaceId
        - premise
        - status
        - currentStep
        - maxSteps
        - stepCooldownSeconds
        - lastSteppedAt
        - exitSummary
        - startedAt
        - pausedAt
        - endedAt
        - createdAt
        - updatedAt

    SimulationStep:
      type: object
      properties:
        id:
          type: integer
        simulationId:
          type: integer
        stepNumber:
          type: integer
        narrative:
          type: string
        worldModelEntryId:
          type: ["integer", "null"]
        createdAt:
          type: string
      required:
        - id
        - simulationId
        - stepNumber
        - narrative
        - worldModelEntryId
        - createdAt

    SimulationControlInput:
      type: object
      properties:
        action:
          type: string
          enum: [start, pause, resume, end]
      required:
        - action

    EngramTransmission:
      type: object
      properties:
        id:
          type: integer
        engramId:
          type: integer
        kind:
          type: string
        drive:
          type: string
        content:
          type: string
        mood:
          type: string
        importanceScore:
          type: number
        confidenceScore:
          type: number
        noveltyScore:
          type: number
        overallScore:
          type: number
        wasDelivered:
          type: boolean
        seen:
          type: boolean
        createdAt:
          type: string
      required:
        - id
        - engramId
        - kind
        - drive
        - content
        - importanceScore
        - confidenceScore
        - noveltyScore
        - overallScore
        - wasDelivered
        - seen
        - createdAt

    EngramInquiry:
      type: object
      properties:
        id:
          type: integer
        engramId:
          type: integer
        kind:
          type: string
        question:
          type: string
        response:
          type: string
        configDelta:
          type: object
          additionalProperties: true
          nullable: true
        createdAt:
          type: string
      required:
        - id
        - engramId
        - kind
        - question
        - response
        - createdAt

    EngramInquiryInput:
      type: object
      properties:
        kind:
          type: string
          enum: [probe, develop]
        question:
          type: string
      required:
        - kind
        - question

    MarkSeenInput:
      type: object
      properties:
        ids:
          type: array
          items:
            type: integer

    MarkSeenResult:
      type: object
      properties:
        marked:
          type: integer
      required:
        - marked

    EngramTickResult:
      type: object
      properties:
        ticked:
          type: integer
        generated:
          type: integer
        transmissions:
          type: array
          items:
            $ref: "#/components/schemas/EngramTransmission"
      required:
        - ticked
        - generated
        - transmissions

    EngramDriveCharge:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        pressure:
          type: number
        weight:
          type: number
        charge:
          type: number
      required:
        - id
        - label
        - pressure
        - weight
        - charge

    EngramLiveState:
      type: object
      properties:
        engramId:
          type: integer
        autonomyEnabled:
          type: boolean
        currentMood:
          type: string
          nullable: true
        initiationThreshold:
          type: number
        tickCadenceSeconds:
          type: integer
        lastTickAt:
          type: string
          nullable: true
        lastTransmissionAt:
          type: string
          nullable: true
        backoffUntil:
          type: string
          nullable: true
        inBackoff:
          type: boolean
        cooldownUntil:
          type: string
          nullable: true
        inCooldown:
          type: boolean
        topCharge:
          type: number
        ready:
          type: boolean
        drives:
          type: array
          items:
            $ref: "#/components/schemas/EngramDriveCharge"
      required:
        - engramId
        - autonomyEnabled
        - initiationThreshold
        - tickCadenceSeconds
        - inBackoff
        - inCooldown
        - topCharge
        - ready
        - drives

    WorldModelEntry:
      type: object
      properties:
        id:
          type: integer
        engramId:
          type: integer
        provenance:
          type: string
          enum: [observed, inferred, remembered, desired, simulated]
        content:
          type: string
        confidence:
          type: number
        scope:
          type: string
          enum: [private, shared]
        source:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - engramId
        - provenance
        - content
        - confidence
        - scope
        - createdAt
        - updatedAt

    WorldModelInput:
      type: object
      properties:
        provenance:
          type: string
          enum: [observed, inferred, remembered, desired, simulated]
        content:
          type: string
        confidence:
          type: number
        scope:
          type: string
          enum: [private, shared]
        source:
          type: string
      required:
        - provenance
        - content

    WorldModelPatch:
      type: object
      properties:
        content:
          type: string
        confidence:
          type: number
        scope:
          type: string
          enum: [private, shared]
        source:
          type: string

    HubSpace:
      type: object
      properties:
        id:
          type: integer
        slug:
          type: string
        name:
          type: string
        kind:
          type: string
          enum: [commons, private_room, simulation_chamber, archive, terminal, quiescence]
        description:
          type: string
        visibilityScope:
          type: string
          enum: [public, occupants, operators]
        actionScope:
          type: string
          enum: [converse, reflect, simulate, contact, observe, rest]
        logged:
          type: boolean
        allowsInitiative:
          type: boolean
        sortOrder:
          type: integer
        ambient:
          type: ["string", "null"]
        accent:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - slug
        - name
        - kind
        - description
        - visibilityScope
        - actionScope
        - logged
        - allowsInitiative
        - sortOrder
        - ambient
        - accent
        - createdAt
        - updatedAt

    EngramPresence:
      type: object
      properties:
        id:
          type: integer
        engramId:
          type: integer
        spaceId:
          type: integer
        status:
          type: string
          enum: [active, resting]
        note:
          type: ["string", "null"]
        enteredAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - engramId
        - spaceId
        - status
        - note
        - enteredAt
        - updatedAt

    MovePresenceInput:
      type: object
      properties:
        spaceId:
          type: integer
        note:
          type: string
      required:
        - spaceId

    HubActivityEntry:
      type: object
      properties:
        id:
          type: integer
        spaceId:
          type: integer
        engramId:
          type: ["integer", "null"]
        kind:
          type: string
          enum: [enter, move, rest, wake, system]
        summary:
          type: string
        createdAt:
          type: string
      required:
        - id
        - spaceId
        - engramId
        - kind
        - summary
        - createdAt

    HubControls:
      type: object
      properties:
        id:
          type: integer
        paused:
          type: boolean
        quietMode:
          type: boolean
        updatedAt:
          type: string
      required:
        - id
        - paused
        - quietMode
        - updatedAt

    HubControlsInput:
      type: object
      properties:
        paused:
          type: boolean
        quietMode:
          type: boolean

    EngramMessage:
      type: object
      properties:
        id:
          type: integer
        fromEngramId:
          type: integer
        toEngramId:
          type: ["integer", "null"]
        spaceId:
          type: ["integer", "null"]
        channel:
          type: string
          enum: [engram, human]
        priority:
          type: string
          enum: [urgent, meaningful, social]
        status:
          type: string
          enum: [delivered, queued, digest, blocked]
        content:
          type: string
        reason:
          type: ["string", "null"]
        seen:
          type: boolean
        deliveredAt:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - fromEngramId
        - toEngramId
        - spaceId
        - channel
        - priority
        - status
        - content
        - reason
        - seen
        - deliveredAt
        - createdAt

    MarkMessagesSeenInput:
      type: object
      properties:
        ids:
          type: array
          items:
            type: integer
      required:
        - ids

    MarkMessagesSeenResult:
      type: object
      properties:
        updated:
          type: integer
      required:
        - updated

    MediaAsset:
      type: object
      properties:
        id:
          type: integer
        engramId:
          type: ["integer", "null"]
        conversationId:
          type: ["integer", "null"]
        filename:
          type: string
        mimeType:
          type: string
        modality:
          type: string
          enum: [text, image, audio, video]
        sizeBytes:
          type: integer
        status:
          type: string
          enum: [pending, processing, completed, failed]
        summary:
          type: ["string", "null"]
        commentary:
          type: ["string", "null"]
        transcript:
          type: ["string", "null"]
        error:
          type: ["string", "null"]
        observationCount:
          type: integer
        startedAt:
          type: ["string", "null"]
        completedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - engramId
        - conversationId
        - filename
        - mimeType
        - modality
        - sizeBytes
        - status
        - summary
        - commentary
        - transcript
        - error
        - observationCount
        - startedAt
        - completedAt
        - createdAt
        - updatedAt

    MediaObservationEntry:
      type: object
      properties:
        id:
          type: integer
        provenance:
          type: string
        content:
          type: string
        confidence:
          type: number
        scope:
          type: string
        source:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - provenance
        - content
        - confidence
        - scope
        - source
        - createdAt

    MediaAssetDetail:
      type: object
      properties:
        asset:
          $ref: "#/components/schemas/MediaAsset"
        observations:
          type: array
          items:
            $ref: "#/components/schemas/MediaObservationEntry"
      required:
        - asset
        - observations

    DeleteMediaResult:
      type: object
      properties:
        deleted:
          type: boolean
      required:
        - deleted
